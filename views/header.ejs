<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/normalize.css">
    <link rel="stylesheet" href="/css/style.css">
    <title><%= nav[section].name %></title>
</head>

<body>
    <header>
        <div>Система электронного голосования</div>
    </header>
    <div class="content">
    
        <nav class="nav">
            <% for (let item in nav) { %>
                <a class="nav-item" href="<%= nav[item].url %>" data-selected="<%= item === section %>">
                    <%= nav[item].name %>
                </a>
            <% } %>
        </nav>

        <div class="title">
            <h1><%= nav[section].name %></h1>
            <% for (let subitem in nav[section].subnav) { %>
                <a class="nav-item" href="<%= `${nav[section].url}${nav[section].subnav[subitem].url}` %>" data-selected="<%=  %>">
                    <%= nav[section].subnav[subitem].name %>
                </a>
            <% } %>
        </div>